numbers = (1, 2, 3)
#parentheses denotes a tuple instead of a list
numbers[0] = 10
print(numbers[0])


#useful for when you want something that cannot be changed. trying to change a tuple with result in an error (see above ^)